"use strict";

/**
 * This will display js modal
 * @param {string} msg the message in the notification
 * @param {string} notificationType notification message type
 */
const BWJSModal = (msg, notificationType = "success") => {
  
};

export { BWJSModal };
